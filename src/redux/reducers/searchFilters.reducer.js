// to be used to store search filter state: toggle switches to filter results for wheelchair accessibility, single-stall, changing table, and open now if possible (might need to make a separate get request to the geocoding)
const searchFilters = () => {
    
}